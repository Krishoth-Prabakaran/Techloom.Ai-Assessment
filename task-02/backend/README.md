# Task 02 API

Express API for product discovery, checkout reservation, mock payment, cancellation, refunds, and order history. Configure `DATABASE_URL` for the production PostgreSQL adapter before deployment; local development is intentionally zero-config.
